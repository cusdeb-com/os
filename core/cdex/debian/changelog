cdex (0.0.5-1cd) trixie; urgency=medium

  * runtime/bin: migrate the CDEX session to a win64 Wine prefix, matching
    Wine 11 where pure win32 prefixes are deprecated:
    - cdex-win32-session: export WINEARCH=win64, wipe prefixes created as
      win32 (no syswow64) by renaming them aside to .legacy-win32 to
      preserve user data, link the ReactOS payload DLLs and .cpl files into
      syswow64 instead of system32, scope the native ReactOS DLL overrides
      to the 32-bit shell processes, and keep only mscoree and
      winemenubuilder.exe in the session-wide WINEDLLOVERRIDES.
    - cdex-registry: register browseui.dll with the 32-bit regsvr32 from
      syswow64, and mirror shell-visible HKLM\Software writes into the
      32-bit registry view (Wow6432Node) via reg /reg:32 — the 32-bit
      reactos-explorer reads Classes CLSID registrations, FontSubstitutes,
      the desktop NameSpace and Winlogon\Shell only from that view, so
      64-bit-only writes left desktop icons blank (white sheets) and the
      shell without the UI font substitution.
    - cdex-display-properties, cdex-shortcut-helper, cdex-wine-cleanup:
      switch WINEARCH to win64; cdex-display-properties now runs the 32-bit
      control.exe by its explicit syswow64 path.
  * Add 0020-taskbar-erase-no-reinvalidate.patch: stop CTaskSwitchWnd from
    invalidating its TBSTYLE_TRANSPARENT toolbar in OnEraseBackground. With
    an active msstyles theme (Wine 11 default Aero/Mizu) the erase handler
    and the toolbar paint re-trigger each other in an infinite repaint loop
    (100% CPU, WM_TIMER starvation: taskbar bridge sync and tray clock
    frozen).
  * Add 0021-uxtheme-export-openthemedatafordpi.patch: export
    OpenThemeDataForDpi from the ReactOS uxtheme. Wine 11 comctl32 resolves
    it at load time and threw a DelayLoadFailure exception inside window
    procs when it was missing.
  * debian/control: require wine (>= 11.15.0-1cd), matching the win64
    session migration.
  * Add 0022-external-task-icons.patch: load host window icons on external
    taskbar buttons from .ico files exported by the bridge (LoadImage,
    instead of ExtractIconEx which downscales via the shell32 icon cache).
    The taskbar image list stays at the upstream ILC_COLOR32; the
    alpha-blending workaround that forced ILC_COLOR24 in 0008 is dropped as
    it does not reproduce on Wine 11. A failed icon load is retried on the
    next sync instead of sticking with the fallback icon.
  * 0002-taskbar-bridge.patch: also write the exact on-screen rect of every
    external task button to reactos-taskbar-buttons.json.
  * cdex-taskbar-bridge: export each window's _NET_WM_ICON to a
    content-hashed .ico under taskbar-icons/ and advertise it in the state
    JSON; the export is cached by the hash of the raw _NET_WM_ICON payload,
    and stale .ico files are pruned once no window references them.
  * cdex-taskbar-bridge: set _NET_WM_ICON_GEOMETRY on task windows, using
    the exact button rects exported by the explorer (falling back to an
    approximation from the window order). The ReactOS taskbar is a Wine
    window and cannot set it, and without it openbox's iconify animation
    slides windows to a default off-screen point, where Wine 11 adopted the
    transient geometry and win32u withdrew the window (window and taskbar
    button vanished on restore). The root size is re-read on every scan so
    a resolution change during login no longer poisons the geometry.
  * cdex-taskbar-bridge: skip oversized _NET_WM_ICON entries instead of
    aborting the whole icon parse.
  * cdex-win32-session: guard configure_reactos_registry so a single
    failing wine reg add does not abort the login session.

 -- Artem Lavrukhin <lavryha4590@gmail.com>  Fri, 21 Aug 2026 13:31:03 +0300

cdex (0.0.4-1cd) trixie; urgency=medium

  * Ship the ReactOS Marlett.ttf font in usr/share/cusdeb/cdex/fonts/ and
    symlink it into the Wine prefix at C:\windows\Fonts. The ReactOS shell
    draws Start Menu submenu arrows and other frame controls via Marlett
    glyphs; without the ReactOS font Wine's built-in Marlett renders glyph
    '8' as a thin "×" instead of a right-pointing arrow.

 -- Artem Lavrukhin <lavryha4590@gmail.com>  Thu, 20 Aug 2026 18:20:51 +0300

cdex (0.0.3-2cd) trixie; urgency=medium

  * cdex-win32-session: stop trying to recreate the reactos-explorer.exe
    symlink on every login. /usr/share/cusdeb/cdex is root-owned, so the
    ln -sf there silently failed with "Permission denied" on every
    session start (masked because it ran as an `if` condition under
    set -e); it was also redundant, since the symlink is already
    shipped by the package.
  * Ship that symlink via debian/cdex-base.links (dh_link) instead of a
    plain ln -sf in the Makefile install rule. The previous approach
    already produced a correctly dpkg-owned symlink (fixed in
    0.0.1-5cd), so this is a packaging-style cleanup, not a fix for an
    unowned file.

 -- Artem Lavrukhin <lavryha4590@gmail.com>  Wed, 19 Aug 2026 20:50:55 +0300

cdex (0.0.3-1cd) trixie; urgency=medium

  * add the CusDeb brand wallpaper: the PNG master lives in
    wallpapers/cusdeb-race.png and is converted to BMP at package build
    time (the desktop paint path loads the wallpaper via LoadImageW,
    which only supports BMP); installed to
    usr/share/cusdeb/cdex/Web/Wallpaper/, following the Web\Wallpaper\
    convention used by Windows.
  * cdex-registry: set the new wallpaper as the default desktop
    background (Wallpaper, WallpaperStyle, TileWallpaper) and drop the
    custom #5D81AB background-color setup (Control Panel\Colors\Background
    and the CustomColors palette entry), so that only the wallpaper is
    installed by default.
  * cdex-registry: also set HKLM\...\CurrentVersion\WallPaperDir to
    C:\reactos\Web\Wallpaper (wine.inf never creates it), so that desk.cpl
    scans the wallpaper directory when filling its background list.
  * recolor the remaining #5D81AB brand backgrounds (the lightdm greeter
    and the xsetroot X root color) to #74A7E2 to match the main
    background of the new wallpaper.

 -- Evgeny Golyshev <eugulixes@cusdeb.com>  Tue, 18 Aug 2026 18:59:59 +0300

cdex (0.0.2-1cd) trixie; urgency=medium

  * cdex-taskbar-bridge: minimize a window when its taskbar button is
    clicked while the window is active (standard taskbar toggle);
    previously the click always activated the window, doing nothing for
    an already-focused one. The last active task window is tracked on
    each poll, since a taskbar click moves X activation to the taskbar
    dock before the command arrives.
  * cdex-taskbar-bridge: re-assert activation when focusing a window if
    it did not stick; the taskbar (or Wine on unminimize) can steal the
    focus back, leaving a restored window unfocused.
  * cdex-taskbar-bridge: poll the control/launch files every 0.1 s
    between window list syncs (still 0.5 s), cutting taskbar click
    latency from up to 0.5 s to ~0.1 s at ~1% CPU.
  * cdex-taskbar-bridge: don't let a missing _NET_WM_NAME clobber the
    WM_NAME fallback, which hid windows without it (e.g. xterm) from
    the taskbar.

 -- Artem Lavrukhin <lavryha4590@gmail.com>  Wed, 12 Aug 2026 16:30:42 +0300

cdex (0.0.1-5cd) trixie; urgency=medium

  * cdex-calc.postrm, mspaint.postrm, spider.postrm, winmine.postrm:
    guard the cdex-shortcut-helper call. It is installed by cdex-base,
    and "Depends: cdex-base" does not guarantee it is still present
    across separate remove/purge transactions, so the unguarded call
    could fail under set -e and leave dpkg --purge in a broken state.
  * Stop creating reactos-explorer.exe in cdex-base.postinst, where it
    was never tracked by cdex-base.install and never cleaned up on
    purge. Its target is constant and known at build time, so it is
    now created during install and owned by dpkg like any other file.

 -- Evgeny Golyshev <eugulixes@cusdeb.com>  Tue, 11 Aug 2026 18:19:09 +0300

cdex (0.0.1-4cd) trixie; urgency=medium

  * debian/control: restore Priority: optional, dropped in 0.0.1-3cd,
    because reprepro requires the field to publish the .deb.

 -- Artem Lavrukhin <lavryha4590@gmail.com>  Thu, 06 Aug 2026 13:15:15 +0300

cdex (0.0.1-3cd) trixie; urgency=medium

  * debian/control: bump Standards-Version to 4.7.4 and drop the
    Priority field, which just spelled out the default.
  * debian/copyright: document the actual per-component licensing of
    the vendored ReactOS tree, its bundled third-party libraries under
    sdk/lib/3rdparty, wallpapers, and fonts, instead of the previous
    blanket GPL-2.0+ claim.
  * runtime/bin: drop the .sh extension from cdex-registry, matching
    every other script, and normalize shebangs to #!/bin/bash /
    #!/usr/bin/python3 throughout.

 -- Evgeny Golyshev <eugulixes@cusdeb.com>  Fri, 31 Jul 2026 18:07:47 +0300

cdex (0.0.1-2cd) trixie; urgency=medium

  * debian/control: use a dedicated Section, matching the convention of
    other desktop environments (GNOME, KDE, Xfce).
  * debian/control: sort and align Depends/Build-Depends.
  * debian/control: rewrite package descriptions.
  * Reorganize package layout: move runtime files under runtime/
    (bin/config/themes/icons split), rename reactos-src/ to src/,
    normalize utility names to a single cdex- prefix, and fix the
    Makefile's stale fontconfig install path.

 -- Evgeny Golyshev <eugulixes@cusdeb.com>  Thu, 30 Jul 2026 14:35:09 +0300

cdex (0.0.1-1cd) trixie; urgency=medium

  * Initial release.

 -- Artem Lavrukhin <lavryha4590@gmail.com>  Wed, 29 Jul 2026 10:42:29 +0300
